{
  "skill_name": "cli-ts",
  "evals": [
    {
      "id": 0,
      "prompt": "Design a provider-neutral TypeScript CLI with nested commands that another coding agent can operate reliably. Include package, output, error, and test contracts.",
      "expected_output": "A minimal architecture and public contract with JSON success, structured stderr errors, provider boundaries, and packed-artifact verification.",
      "files": [],
      "assertions": [
        {"id": "cli-architecture", "text": "Separates thin command registration from core and provider services"},
        {"id": "cli-errors", "text": "Defines stable code, message, retryable, and ordered next_steps fields"},
        {"id": "cli-package", "text": "Tests the installed tarball rather than only source code"}
      ]
    },
    {
      "id": 1,
      "prompt": "Our GitHub Action publishes an npm CLI with NPM_TOKEN. Replace this with the safest current flow and specify the release checks.",
      "expected_output": "An npm trusted publishing plan with least privilege, exact OIDC requirements, artifact verification, registry verification, and GitHub Release creation.",
      "files": [],
      "assertions": [
        {"id": "cli-oidc", "text": "Uses npm trusted publishing with id-token write and no long-lived publish token"},
        {"id": "cli-release", "text": "Verifies tag and package versions before publishing"},
        {"id": "cli-release-artifact", "text": "Publishes the same packed artifact that was tested"}
      ]
    },
    {
      "id": 2,
      "prompt": "Add one flat command with two flags to a small existing TypeScript CLI. Decide whether to add Commander, oclif, or use Node itself.",
      "expected_output": "A repository-first decision that preserves the current parser and prefers Node util.parseArgs when no parser exists and the command remains flat.",
      "files": [],
      "assertions": [
        {"id": "cli-reuse", "text": "Inspects and preserves the existing parser and module format"},
        {"id": "cli-yagni", "text": "Avoids oclif unless plugins, installers, or autoupdate are required"}
      ]
    },
    {
      "id": 3,
      "prompt": "Migrate a tag-driven npm CLI release to Release Please. The last published version is 0.3.0, package.json already says 0.4.0, and npm trusted publishing is bound to publish.yml and environment npm. Make version bumps, GitHub Releases, and OIDC publication reliable.",
      "expected_output": "A single-workflow Release Please plan that bootstraps from 0.3.0, preserves the trusted publisher binding, avoids duplicate release ownership, and accounts for GITHUB_TOKEN trigger behavior.",
      "files": [],
      "assertions": [
        {"id": "cli-release-bootstrap", "text": "Initializes the manifest from the last published version rather than the unreleased package.json version"},
        {"id": "cli-release-reconcile", "text": "Checks commits since the published tag to reconcile the expected next version with the unreleased package.json version"},
        {"id": "cli-release-same-run", "text": "Gates validation and npm publication on release_created in the same workflow run"},
        {"id": "cli-release-token", "text": "Requires the Actions pull-request setting, explains approval-required PR CI, and recommends a GitHub App token only for unattended CI or auto-merge"},
        {"id": "cli-release-owner", "text": "Leaves version, changelog, tag, and GitHub Release creation to Release Please"}
      ]
    },
    {
      "id": 4,
      "prompt": "Investigate npm audit findings in an existing TypeScript CLI and preview a safe remediation. The audit reports vulnerabilities with a non-zero exit code, and a prior script piped JSON directly into a parser while discarding stderr.",
      "expected_output": "A package-manager-preserving audit workflow that captures stdout, stderr, and exit status separately, validates JSON, distinguishes findings from command failure, previews remediation, and verifies the resulting lockfile.",
      "files": [],
      "assertions": [
        {"id": "cli-audit-capture", "text": "Captures npm audit JSON and exit status separately without hiding stderr"},
        {"id": "cli-audit-exit", "text": "Treats a non-zero audit exit as possible findings rather than automatically as malformed output"},
        {"id": "cli-audit-errors", "text": "Validates the vulnerability report shape and surfaces stderr when audit output indicates a command failure"},
        {"id": "cli-audit-preview", "text": "Treats audit fix dry-run output as human evidence unless the active npm version is verified to emit clean JSON"},
        {"id": "cli-audit-fix", "text": "Reviews the manifest and lockfile diff and reruns install, tests, and audit after remediation"},
        {"id": "cli-audit-force", "text": "Avoids npm audit fix --force unless a dependency-range change is explicitly approved"}
      ]
    }
  ]
}
