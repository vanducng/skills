{
  "name": "show-off-scripts",
  "version": "1.0.0",
  "description": "Parallel section capture scripts for show-off skill",
  "type": "module",
  "scripts": {},
  "dependencies": {
    "puppeteer": "^24.10.2"
  },
  "optionalDependencies": {
    "sharp": "^0.33.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "note": "Run npm install in this directory before first local capture. sharp is optional and only used for oversized image compression."
}
