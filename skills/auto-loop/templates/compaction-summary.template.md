# Auto-loop resumed from compaction

## Goal
{GOAL}

## Progress so far
- Iterations: {ITERATION}
- Last status: {STATUS}
- Verifier: {VERIFIER_RESULT}
- Audit: {AUDIT_VOTE}
- Restarts so far: {RESTART_COUNT}

## Recent verifier output (last 30 lines)
```
{VERIFIER_TAIL}
```

## Recent audit feedback (last 3)
```
{AUDIT_TAIL}
```

## Files most-changed
```
{FILES_HOT_LIST}
```

## Next action
{NEXT_ACTION}

## Continue
The full prior-session transcript is intentionally NOT included. Work from this
summary, the repo's current state, and `.auto-loop/goal-state.json` (which is the
source of truth for iteration count and status).

When you stop this iteration, update `.auto-loop/goal-state.json` per the schema
in `{SKILL_DIR}/state-schema.json`.
